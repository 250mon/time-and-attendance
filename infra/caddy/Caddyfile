{
	auto_https disable_redirects
}

{$DOMAIN:localhost}:{$HTTPS_PORT:443} {
	handle_path /api/* {
		reverse_proxy backend:8000
	}

	handle {
		reverse_proxy frontend:3000
	}
}

http://{$DOMAIN:localhost}:{$HTTP_PORT:80} {
	redir https://{host}:{$HTTPS_PORT:443}{uri} permanent
}
